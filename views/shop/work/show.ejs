<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Worker Details | AGRO</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* Your existing CSS remains the same */
    :root {
      --primary: #2e7d32;
      --primary-dark: #1b5e20;
      --primary-light: #a5d6a7;
      --accent: #ff9800;
      --danger: #e53935;
      --danger-dark: #c62828;
      --text-dark: #212121;
      --text-muted: #616161;
      --bg-light: #f8f9fa;
      --card-bg: #ffffff;
      --border-radius: 16px;
      --shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
      --transition: all 0.3s ease;
    }

    body {
      font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
      background: linear-gradient(135deg, #e8f5e9, #f1f8e9);
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: var(--text-dark);
    }

    .worker-card {
      background-color: var(--card-bg);
      border-radius: var(--border-radius);
      box-shadow: var(--shadow);
      padding: 2.5rem;
      max-width: 700px;
      width: 90%;
      position: relative;
      overflow: hidden;
    }

    .worker-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 5px;
      background: linear-gradient(to right, var(--primary), var(--primary-light));
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    h1 {
      color: var(--primary-dark);
      font-size: 1.8rem;
      font-weight: 700;
      margin: 0;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    h1 i {
      color: var(--primary);
    }

    .worker-id {
      background-color: var(--primary-light);
      color: var(--primary-dark);
      padding: 5px 12px;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 500;
    }

    .worker-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      margin-bottom: 2rem;
    }

    .worker-avatar {
      border-radius: var(--border-radius);
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      height: 300px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, var(--primary), var(--primary-dark));
      color: white;
      font-size: 4rem;
    }

    .worker-details {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .detail-item {
      padding-bottom: 1rem;
      border-bottom: 1px solid #f0f0f0;
    }

    .detail-item:last-child {
      border-bottom: none;
    }

    .detail-label {
      font-weight: 600;
      color: var(--text-muted);
      font-size: 0.9rem;
      margin-bottom: 0.5rem;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .detail-label i {
      color: var(--primary);
      width: 20px;
    }

    .detail-value {
      font-size: 1.1rem;
      color: var(--text-dark);
    }

    .salary {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--primary-dark);
    }

    .btn-group {
      display: flex;
      gap: 1rem;
      justify-content: flex-end;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 0.8rem 1.5rem;
      font-size: 1rem;
      font-weight: 600;
      border: none;
      border-radius: var(--border-radius);
      cursor: pointer;
      transition: var(--transition);
      text-decoration: none;
      text-align: center;
    }

    .btn-danger {
      background: linear-gradient(135deg, var(--danger), var(--danger-dark));
      color: white;
    }

    .btn-danger:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 15px rgba(229, 57, 53, 0.3);
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary), var(--primary-dark));
      color: white;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 15px rgba(46, 125, 50, 0.3);
    }

    .btn-secondary {
      background-color: white;
      color: var(--text-dark);
      border: 1px solid #e0e0e0;
    }

    .btn-secondary:hover {
      background-color: #f5f5f5;
      transform: translateY(-2px);
    }

    @media (max-width: 768px) {
      .worker-content {
        grid-template-columns: 1fr;
        gap: 1.5rem;
      }
      
      .worker-card {
        padding: 1.5rem;
      }
      
      .btn-group {
        flex-direction: column;
      }
      
      .header {
        flex-direction: column;
        align-items: flex-start;
        gap: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="worker-card">
    <div class="header">
      <h1><i class="fas fa-hard-hat"></i> Worker Details</h1>
      <div class="worker-id">ID: <%= worker._id %></div>
    </div>
    
    <div class="worker-content">
      <div class="worker-avatar">
        <%= worker.name ? worker.name.charAt(0).toUpperCase() : 'W' %>
      </div>
      
      <div class="worker-details">
        <div class="detail-item">
          <div class="detail-label">
            <i class="fas fa-user"></i> Full Name
          </div>
          <div class="detail-value"><%= worker.name || 'Not provided' %></div>
        </div>
        
        <div class="detail-item">
          <div class="detail-label">
            <i class="fas fa-phone"></i> Contact Number
          </div>
          <div class="detail-value"><%= worker.contact || 'Not provided' %></div>
        </div>
        
        <div class="detail-item">
          <div class="detail-label">
            <i class="fas fa-briefcase"></i> Role
          </div>
          <div class="detail-value"><%= worker.role || 'Not specified' %></div>
        </div>
        
        <div class="detail-item">
          <div class="detail-label">
            <i class="fas fa-user-clock"></i> Age
          </div>
          <div class="detail-value"><%= worker.age || 'Not specified' %></div>
        </div>
        
        <div class="detail-item">
          <div class="detail-label">
            <i class="fas fa-money-bill"></i> Salary
          </div>
          <div class="detail-value salary">₹<%= worker.salary || 0 %></div>
        </div>
        
        <div class="detail-item">
          <div class="detail-label">
            <i class="fas fa-calendar"></i> Join Date
          </div>
          <div class="detail-value">
            <% if (worker.createdAt) { %>
              <%= new Date(worker.createdAt).toLocaleDateString() %>
            <% } else { %>
              Not specified
            <% } %>
          </div>
        </div>
        
        <div class="detail-item">
          <div class="detail-label">
            <i class="fas fa-clock"></i> Last Updated
          </div>
          <div class="detail-value">
            <% if (worker.updatedAt) { %>
              <%= new Date(worker.updatedAt).toLocaleDateString() %>
            <% } else { %>
              Not specified
            <% } %>
          </div>
        </div>
      </div>
    </div>
    
    <div class="btn-group">
      <form method="POST" action="/worker/<%= worker._id %>?_method=DELETE" style="display: inline;">
        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this worker?')">
          <i class="fas fa-trash"></i> Delete Worker
        </button>
      </form>
      
      <a href="/worker/<%= worker._id %>/edit" class="btn btn-primary">
        <i class="fas fa-edit"></i> Edit Worker
      </a>
      
      <a href="/worker" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Back to Workers
      </a>
    </div>
  </div>
</body>
</html>